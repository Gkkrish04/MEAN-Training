//we using interface its same like class, its looks like as object it cant be initiated like contract, basically we create the model file for the common object its like a module blue print.

export interface Post {
    title: string;
    content: string;
}